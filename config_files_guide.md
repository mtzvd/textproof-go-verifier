# 🛠️ Конфигурационные файлы TextProof

Этот документ объясняет назначение каждого конфигурационного файла в проекте.

---

## 📋 Список файлов

### 1. **Taskfile.yml** - Task runner (рекомендуется)

**Что это:** Современный task runner, альтернатива Makefile  
**Установка:** `go install github.com/go-task/task/v3/cmd/task@latest`  
**Использование:** `task <команда>`

**Основные команды:**

```bash
task                    # Показать справку
task setup             # Полная настройка проекта
task run               # Запустить сервер
task dev               # Режим разработки (hot reload)
task test              # Запустить тесты
task build             # Собрать приложение
task clean             # Очистка
```

**Преимущества:**

- ✅ Кроссплатформенность (работает на Windows/Linux/macOS одинаково)
- ✅ Простой YAML синтаксис
- ✅ Параллельное выполнение задач
- ✅ Зависимости между задачами
- ✅ Кэширование результатов

---

### 2. **Makefile** - Классический Makefile

**Что это:** Традиционный build tool для Unix-систем  
**Установка:** Обычно уже установлен на Linux/macOS, для Windows: `choco install make`  
**Использование:** `make <команда>`

**Основные команды:**

```bash
make                    # Показать справку
make setup             # Полная настройка проекта
make run               # Запустить сервер
make dev               # Режим разработки (hot reload)
make test              # Запустить тесты
make build             # Собрать приложение
make clean             # Очистка
```

**Когда использовать:**

- ✅ Если привыкли к Makefile
- ✅ Для CI/CD (часто make уже установлен)
- ✅ Для команд с шаблонами (pattern rules)

**Ограничения:**

- ❌ Проблемы на Windows (требует установки)
- ❌ Более сложный синтаксис

---

### 3. **.air.toml** - Air hot reload

**Что это:** Конфигурация для Air - live reload для Go приложений  
**Установка:** `go install github.com/air-verse/air@latest`  
**Использование:** `air` или `make dev` или `task dev`

**Что делает:**

- Следит за изменениями в `.go` и `.templ` файлах
- Автоматически перегенерирует templ (`templ generate`)
- Перезапускает сервер при изменениях
- Показывает ошибки сборки

**Когда использовать:**

- ✅ Активная разработка
- ✅ Нужен быстрый feedback loop
- ✅ Работа с UI (видите изменения сразу)

**Как работает:**

```text
1. Изменили файл .templ или .go
2. Air засекает изменение
3. Запускает pre_cmd: templ generate
4. Пересобирает приложение
5. Перезапускает сервер
```

---

### 4. **modd.conf** - Modd hot reload

**Что это:** Альтернативный hot reload с более гибкой конфигурацией  
**Установка:** `go install github.com/cortesi/modd/cmd/modd@latest`  
**Использование:** `modd` или `make dev-modd` или `task dev-modd`

**Отличия от Air:**

- Более гибкая конфигурация (разные команды для разных файлов)
- Может запускать тесты при изменениях
- Может регенерировать Swagger автоматически
- Более сложная настройка

**Когда использовать:**

- ✅ Нужна сложная логика перезагрузки
- ✅ Хотите разные действия для разных файлов
- ✅ Нужна автоматическая регенерация Swagger

**Текущая конфигурация:**

```text
*.templ        → templ generate
*.go (API)     → swagger regenerate
*.go + *.templ → restart server
```

---

### 5. **.gitignore** - Игнорируемые файлы Git

**Что это:** Список файлов/папок, которые Git не должен отслеживать

**Что игнорируется:**

- Сгенерированные файлы (`*_templ.go`, `docs/`)
- Build артефакты (`build/`, `tmp/`)
- Данные приложения (`data/`)
- IDE конфиги (`.vscode/`, `.idea/`)
- Логи и coverage отчёты
- OS файлы (`.DS_Store`, `Thumbs.db`)

---

## 🎯 Какой инструмент выбрать?

### Для ежедневной разработки

**Taskfile.yml** (через `task`)

- Кроссплатформенный
- Простой синтаксис
- Быстрый

### Для hot reload

**Air** (через `.air.toml`)

- Проще настроить
- Меньше ресурсов
- Достаточно для большинства случаев

### Для CI/CD

**Makefile**

- Обычно уже установлен на build серверах
- Стандарт индустрии

---

## 📚 Примеры использования

### Сценарий 1: Первый запуск проекта

```bash
# Вариант A: Task (рекомендуется)
task setup
task run

# Вариант B: Make
make setup
make run
```

### Сценарий 2: Разработка с hot reload

```bash
# Вариант A: Air (проще)
task dev
# или
air

# Вариант B: Modd (больше функций)
task dev-modd
# или
modd
```

### Сценарий 3: Перед коммитом

```bash
# Вариант A: Task
task fmt
task test

# Вариант B: Make
make pre-commit
```

### Сценарий 4: Production build

```bash
# Вариант A: Task
task prod-build

# Вариант B: Make
make prod-build
```

---

## 🔧 Установка инструментов

### Все сразу

```bash
# Task
task install-tools

# Make
make install-tools
```

### По отдельности

```bash
# Task runner
go install github.com/go-task/task/v3/cmd/task@latest

# Templ
go install github.com/a-h/templ/cmd/templ@latest

# Swagger
go install github.com/swaggo/swag/cmd/swag@latest

# Air
go install github.com/air-verse/air@latest

# Modd
go install github.com/cortesi/modd/cmd/modd@latest
```

---

## ⚙️ Настройка под себя

### Изменить порт

**Taskfile.yml:**

```yaml
vars:
  PORT: 9090  # изменить здесь
```

**Makefile:**

```makefile
PORT := 9090  # изменить здесь
```

**.air.toml:**

```toml
args_bin = ["-port", "9090", "-difficulty", "4"]
```

**modd.conf:**

```text
daemon +sigterm: go run cmd/server/main.go -port 9090 -difficulty 4
```

### Изменить сложность майнинга

Аналогично, поменять `DIFFICULTY` или `-difficulty` параметр.

---

## 🐛 Troubleshooting

### Task: command not found

```bash
go install github.com/go-task/task/v3/cmd/task@latest
export PATH=$PATH:$(go env GOPATH)/bin
```

### Air/Modd не работает

```bash
# Проверить установку
air --version
modd --version

# Переустановить
go install github.com/air-verse/air@latest
go install github.com/cortesi/modd/cmd/modd@latest
```

### Templ generate не запускается

```bash
# Установить
go install github.com/a-h/templ/cmd/templ@latest

# Проверить
templ version
```

---

## 📊 Сравнение инструментов

| Функция | Task | Make | Air | Modd |
|---------|------|------|-----|------|
| Кроссплатформенность | ✅ | ⚠️ | ✅ | ✅ |
| Простота | ✅ | ⚠️ | ✅ | ⚠️ |
| Hot reload | ❌ | ❌ | ✅ | ✅ |
| Параллельность | ✅ | ✅ | ❌ | ⚠️ |
| Гибкость | ✅ | ✅ | ⚠️ | ✅ |

**Легенда:** ✅ Отлично | ⚠️ С ограничениями | ❌ Нет

---

## 🎯 Рекомендации

### Для новичков

**Task + Air**

- `task run` для обычного запуска
- `task dev` для разработки с hot reload

### Для опытных

**Make + Modd**

- Больше контроля
- Сложнее, но мощнее

### Для команды

**Task + Air + Makefile**

- Task для разработки (удобнее)
- Make для CI/CD (совместимость)

---

## 📝 Дополнительные команды

Смотрите полный список команд:

```bash
task --list          # Task
make help            # Make
```

Каждый файл хорошо задокументирован внутри, читайте комментарии!
